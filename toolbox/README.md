# Toolbox Environment

Containerized dev environment using Fedora toolbox.

## Steps to build and enter toolbox:
1. Run `toolbox-launcher.sh` to enter the container with Dotfiles mounted.
2. Run `install.sh` to install additional tools if needed.

## Build the toolbox container:
toolbox create --container devbox

## Enter the toolbox container:
toolbox enter devbox
